<!DOCTYPE html>
<html>
<head>
  <style>
    /* Set the size of the div element that contains the map */
   #map {
     height: 400px;  /* The height is 400 pixels */
     width: 100%;  /* The width is the width of the web page */
    }
 </style>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

<div class="jumbotron text-center">
  <div class="container">
    <a href="/" class="restaurant-logo">
      <img src="/restaurant-logo.png">
    </a>
    <h1>Food Finder</h1>
    <p>Ever struggling to find somewhere to eat? Hate the inconvenience of having to make reservations? Food Finder shows you all restaurants near your location and allows you to make reservations nice and easy!</p>
    <label for="address">Address:</label>
    <input type="text" class="form-control" id="address" placeholder="Enter Address">
    <a type="button" class="btn btn-lg btn-default" href="https://devcenter.heroku.com/articles/getting-started-with-nodejs"><span class="glyphicon glyphicon-search"></span> Search For Restaurants</a>
  </div>
</div>
<div class="container">
  <div id="map"></div>
  <script>
    // Initialize and add the map
    function initMap(location) {
      // The location entered by the user
      var loc = {lat: 43.945801, lng: -78.894600};
      // The map, centered at the location entered by the user
      var map = new google.maps.Map(
        document.getElementById('map'), {zoom: 12, center: loc});
      // The marker, positioned at location entered by the user
      var marker = new google.maps.Marker({position: loc, map: map});
    }
  </script>
</div>
<!--Load the API from the specified URL
    * The async attribute allows the browser to render the page while the API loads
    * The key parameter will contain your own API key (which is not needed for this tutorial)
    * The callback parameter executes the initMap() function
-->
<script async defer
  src="https://maps.googleapis.com/maps/api/js?key=AIzaSyA7kd_beDHhWfpMdLQmmyzhROakvpJuNKE&callback=initMap">
</script>
</body>
</html>
